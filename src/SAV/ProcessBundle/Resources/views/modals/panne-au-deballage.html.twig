<div class="modal modal-panne-deballage">
    <div class="modal-background"></div>
    <div class="modal-content">
        <div class="modal-title">
            <h2>Attention : Panne au déballage</h2>
        </div>
        <div class="modal-container">
            <div class="notification is-warning">
                <i class="material-icons">info</i>
                <span>{{ 'Mettre le produit dans le stock à détruire' | upper}}</span>
            </div>
            <div class="modal-actions">
                <a href="{{ path('treatment_product_splitting', {'numeroBar': produit.numeroBar}) }}" class="btn-large waves-effect waves-light btn-splitting">
                    {{ 'Récupération de pièces détachées' | upper }}
                </a>
                <a href="{{ path('treatment_product_splitting', {'numeroBar': produit.numeroBar}) }}" class="btn-large waves-effect waves-light btn-close-treatment">
                    {{ 'Clore le traitement produit' | upper }}
                </a>
            </div>
        </div>
    </div>
    </div>
</div>
